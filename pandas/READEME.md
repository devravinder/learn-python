# Pandas

## Set up

### From VSC
  >
  > Note: open only this folder in vsc

1. `python3 -m venv venv`
2. `source venv/bin/activate`
3. `pip install pandas`
4. `pip freeze > requirements.txt`
5. run `python3 main.py` or use `.ipynb` files
6. `deactivate`
